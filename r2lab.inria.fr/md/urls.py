from django.conf.urls import url

from . import views

# xxx not used in fact, see r2lab/urls.py that manages this directly
urlpatterns = [
#    url('^(?P<markdown_file>.*)$', views.markdown_page, name='show'),
    ]
